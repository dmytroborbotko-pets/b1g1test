import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.testb1g1.app",
  appName: "testb1g1",
  webDir: "out",
  server: {
    androidScheme: "https",
  },
};

export default config;
